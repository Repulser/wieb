<UserControl x:Class="Dictionary.UrbanDicionaryPanel.Views.UrbanDicionaryPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodels="clr-namespace:Dictionary.UrbanDicionaryPanel.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             Background="AliceBlue">
    
    <UserControl.Resources>
        <viewmodels:TextBoxGetter x:Key="TextBoxGetter" />
    </UserControl.Resources>

    <StackPanel>
        <TextBox x:Name="TextBox" Text="{Binding Source={StaticResource TextBoxGetter}, Path=TextBoxText, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}" />
        <Label x:Name="Label" />
        <Button x:Name="Button" />
    </StackPanel>
</UserControl>
